version: "2"

run:
  timeout: 5m
  tests: true

linters:
  default: none
  enable:
    - errcheck
    - gocritic
    - gocyclo
    - ineffassign
    - misspell
    - staticcheck
    - unconvert
    - unused
  settings:
    gocyclo:
      min-complexity: 5
  exclusions:
    paths:
      - gen
